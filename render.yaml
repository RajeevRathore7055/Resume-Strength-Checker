services:
  - type: web

    name: resume-matcher

    runtime: python

    buildCommand: |
      cd Backend
      pip install -r requirements.txt

    startCommand: |
      cd Backend
      uvicorn main:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: DATABASE_URL
        sync: false
